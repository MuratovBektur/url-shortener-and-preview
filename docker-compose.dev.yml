name: url-shortener-and-preview_dev

services:
  postgres:
    image: postgres:16-alpine
    container_name: "url-shortener-and-preview_postgres_dev"
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: url_shortener
    expose:
      - 5432
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 10

  server:
    container_name: "url-shortener-and-preview_server_dev"
    restart: always
    build: server
    expose:
      - 5000
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/url_shortener
      - PORT=5000
      - BASE_URL=http://localhost
    depends_on:
      postgres:
        condition: service_healthy

  nginx:
    image: nginx
    restart: "no"
    container_name: "url-shortener-and-preview_nginx_dev"
    ports:
      - 80:80
    volumes:
      - ./client/dist:/usr/share/nginx/html
      - /etc/localtime:/etc/localtime:ro
      - ./nginx/dev:/etc/nginx/conf.d
    depends_on:
      - server

volumes:
  postgres_data: